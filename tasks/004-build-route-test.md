# Task-004: 빌드 검증 및 라우팅 테스트

## 개요

`npm run build` 프로덕션 빌드 통과 확인 및 Playwright MCP로 전체 라우트 네비게이션 동작 검증.

## 완료 일자

2026-05-19

## 수락 기준

- [x] `npm run build` 오류 없이 완료
- [x] 공개 라우트 4개 정상 렌더링
- [x] 보호 라우트 3개 미인증 시 `/auth/login` 리다이렉트
- [x] 헤더 네비게이션 링크 정상 동작
- [x] 로그인 후 보호 라우트 접근 가능 (사용자 직접 확인)

## 검증 결과

### 빌드

- `npm run build` 성공 — TypeScript/ESLint 오류 없음, 15개 라우트 모두 Dynamic 빌드

### Playwright MCP 검증 (2026-05-19)

| 라우트                     | 결과                        |
| -------------------------- | --------------------------- |
| `/`                        | ✅ 200, 헤더 정상           |
| `/events`                  | ✅ 200, 헤더 정상           |
| `/events/test-id`          | ✅ 200, 헤더 정상           |
| `/invite/test-token`       | ✅ 200, 헤더 정상           |
| `/events/new` (미인증)     | ✅ `/auth/login` 리다이렉트 |
| `/profile` (미인증)        | ✅ `/auth/login` 리다이렉트 |
| MOIDA 로고 클릭 → `/`      | ✅ 정상 이동                |
| 로그인 후 보호 라우트 접근 | ✅ 사용자 직접 확인 완료    |

### 추가 변경 사항 (검증 중 발견)

- 헤더 Logout 버튼 제거 → 프로필 페이지로 이동
- 비로그인 헤더 로그인 링크 → Button 컴포넌트로 스타일 통일
