# Task-008: 다크/라이트 테마 적용

## 완료 산출물

- `app/layout.tsx` — ThemeProvider (next-themes) 래핑, defaultTheme="system"
- `components/layout/RootShell.tsx` — /admin 경로 분기 클라이언트 쉘
- 모든 컴포넌트에 `dark:` Tailwind 클래스 적용

## 완료일

2026-05-19
